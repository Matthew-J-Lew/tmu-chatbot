fastapi==0.115.0
uvicorn[standard]==0.30.6

# Async DB + pooling
asyncpg==0.29.0

# HTTP client to Ollama
httpx==0.27.0

# Azure OpenAI client (via OpenAI Python SDK)
openai>=1.40.0

# Redis cache (async)
redis==5.0.8

# ML stack (existing)
sentence-transformers>=2.2.2

# Optional but useful for robust retries/backoff if you want later
# tenacity==8.5.0
